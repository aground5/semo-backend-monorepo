apiVersion: v1
kind: Secret
metadata:
  name: {{ include "oxhr-authn-server.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "oxhr-authn-server.labels" . | nindent 4 }}
type: Opaque
data:
  config.yaml: |-
    {{ tpl (.Files.Get "config.yaml") . | b64enc }}